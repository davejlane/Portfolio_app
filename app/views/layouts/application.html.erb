<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Dave Lane - Portfolio" %></title>

    <link href='http://fonts.googleapis.com/css?family=PT+Serif:400,700|Oswald:400,700|Patua+One' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar="">
      <ul class="title-area row">
        <li class="name">
          <h1 class='logo'><a href="/">Dave Lane - Web Developer</a></h1>
        </li>
      </ul>
    </nav>
    <div class="row main">
      <div class="small-12 columns">
        <% flash.each do |key, value| %>
          <div class="alert-box round <%= flash_class(key) %>">
                <%= value %>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>

    <%= javascript_include_tag "application" %>
  </body>
</html>
